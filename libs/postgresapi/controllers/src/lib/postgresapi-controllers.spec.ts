import { postgresapiControllers } from './postgresapi-controllers';

describe('postgresapiControllers', () => {
  it('should work', () => {
    expect(postgresapiControllers()).toEqual('postgresapi-controllers');
  });
});
