import { todomongoapiSchemas } from './todomongoapi-schemas';

describe('todomongoapiSchemas', () => {
  it('should work', () => {
    expect(todomongoapiSchemas()).toEqual('todomongoapi-schemas');
  });
});
