import { todomongoapiControlers } from './todomongoapi-controlers';

describe('todomongoapiControlers', () => {
  it('should work', () => {
    expect(todomongoapiControlers()).toEqual('todomongoapi-controlers');
  });
});
