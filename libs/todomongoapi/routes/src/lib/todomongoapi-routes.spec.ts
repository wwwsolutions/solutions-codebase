import { todomongoapiRoutes } from './todomongoapi-routes';

describe('todomongoapiRoutes', () => {
  it('should work', () => {
    expect(todomongoapiRoutes()).toEqual('todomongoapi-routes');
  });
});
