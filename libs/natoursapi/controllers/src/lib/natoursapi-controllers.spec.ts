import { natoursapiControllers } from './natoursapi-controllers';

describe('natoursapiControllers', () => {
  it('should work', () => {
    expect(natoursapiControllers()).toEqual('natoursapi-controllers');
  });
});
